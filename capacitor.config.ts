import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.duy.ionic-angular',
  appName: 'capacitor-ionic-app',
  webDir: 'www'
};

export default config;
